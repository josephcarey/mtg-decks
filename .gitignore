# Secrets (studio standard — never commit)
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Dependencies
node_modules/

# Test coverage
coverage/

# Scryfall bulk-data cache (regenerated via `bun run deck fetch-bulk`)
data/

# Derived SQLite database (regenerated via `bun run deck build-db`)
*.db
